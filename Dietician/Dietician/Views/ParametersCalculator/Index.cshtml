@model Parameters

@{
    ViewData["Title"] = "Obliczanie parametrów";
}

@using (Html.BeginForm())
{
    <div class="container">
        @Html.Partial("Parameters", Model, new ViewDataDictionary(ViewContext.ViewData) { TemplateInfo = { HtmlFieldPrefix = "Parameters" } })
    </div>

    @if (Model.ShowResults)
    {
        <div class="container">
            @Html.Partial("Results", Model, new ViewDataDictionary(ViewContext.ViewData) { TemplateInfo = { HtmlFieldPrefix = "Parameters" } })
        </div>
    }
}